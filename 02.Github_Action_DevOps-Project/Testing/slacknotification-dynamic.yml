name: Dynamic Slack Notification

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  repository_dispatch:  workflow_call:
    types: [maven-workflow-completed]nputs:
atus:
jobs:us of the calling workflow'
  build:    required: true
    runs-on: ubuntu-latestfault: 'success'
    
    steps:  workflow_name:
    - uses: actions/checkout@v2ing workflow'
    w'
    # - name: Run a Hello World script    required: false
    #   run: echo "Hello, World!"
    
    - name: Get current timeription: 'Repository of the calling workflow'
      id: timestamp
      run: |
        echo "time=$(date +%s)" >> $GITHUB_OUTPUT    type: string
        echo "formatted_time=$(date +'%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_OUTPUT
    'Actor who triggered the calling workflow'
    - name: Set dynamic notification variablesult: ${{ github.actor }}
      id: notify_vars
      run: |
        # Set status based on repository_dispatch payload or job outcome
        if [[ "${{ github.event.client_payload.status }}" == "success" || "${{ job.status }}" == "success" ]]; then
          echo "color=#36a64f" >> $GITHUB_OUTPUT
          echo "pretext=✅ Pipeline Completed Successfully 🎉" >> $GITHUB_OUTPUT
          echo "text=*The pipeline has completed successfully!* :rocket: :white_check_mark:" >> $GITHUB_OUTPUT: string
          echo "status_value=Success ✅" >> $GITHUB_OUTPUT
        else
          echo "color=#FF0000" >> $GITHUB_OUTPUT
          echo "pretext=❌ Pipeline Failed ⚠️" >> $GITHUB_OUTPUT
          echo "text=The pipeline has failed! Please check the logs for more details. :warning:" >> $GITHUB_OUTPUTpe: string
          echo "status_value=Failed ❌" >> $GITHUB_OUTPUT
        fi
    
    - name: Send dynamic Slack notification
      if: always() # Will run regardless of job status
      uses: 8398a7/action-slack@v3
      with:
        status: custom
        fields: workflow,job,commit,repo,ref,author,tooke: Run a Hello World script
        custom_payload: |rld!"
          {
            "attachments": [
              {
                "color": "${{ steps.notify_vars.outputs.color }}",
                "pretext": "${{ steps.notify_vars.outputs.pretext }}",
                "author_name": "${{ github.event.client_payload.actor || github.actor }}",
                "title": "${{ github.event.client_payload.workflow_name || github.workflow }}",
                "title_link": "https://github.com/${{ github.event.client_payload.repo || github.repository }}/actions/runs/${{ github.event.client_payload.run_id || github.run_id }}",ification variables
                "text": "${{ steps.notify_vars.outputs.text }}",rs
                "fields": [
                  {
                    "title": "Repository",| job.status }}" == "success" ]; then
                    "value": "${{ github.event.client_payload.repo || github.repository }}",r=#36a64f" >> $GITHUB_OUTPUT
                    "short": truetext=✅ Pipeline Completed Successfully 🎉" >> $GITHUB_OUTPUT
                  }, completed successfully!* :rocket: :white_check_mark:" >> $GITHUB_OUTPUT
                  {PUT
                    "title": "Branch",
                    "value": "${{ github.event.client_payload.ref_name || github.ref_name }}",r=#FF0000" >> $GITHUB_OUTPUT
                    "short": truetext=❌ Pipeline Failed ⚠️" >> $GITHUB_OUTPUT
                  },d! Please check the logs for more details. :warning:" >> $GITHUB_OUTPUT
                  {UTPUT
                    "title": "Executed by",
                    "value": "${{ github.event.client_payload.actor || github.actor }}",
                    "short": truenamic Slack notification
                  },b status
                  {
                    "title": "Execution time",
                    "value": "${{ steps.timestamp.outputs.formatted_time }}",om
                    "short": truekflow,job,commit,repo,ref,author,took
                  },
                  {
                    "title": "Status",
                    "value": "${{ steps.notify_vars.outputs.status_value }}",
                    "short": trueolor": "${{ steps.notify_vars.outputs.color }}",
                  }y_vars.outputs.pretext }}",
                ],
                "footer": "GitHub Actions",b.workflow }}",
                "footer_icon": "https://github.githubassets.com/favicon.ico", "title_link": "https://github.com/${{ inputs.repo || github.repository }}/actions/runs/${{ inputs.run_id || github.run_id }}",
                "ts": "${{ steps.timestamp.outputs.time }}"   "text": "${{ steps.notify_vars.outputs.text }}",
              }     "fields": [
            ]        {
          }


        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}      env:                    "value": "${{ inputs.repo || github.repository }}",
                    "short": true
                  },
                  {
                    "title": "Branch",
                    "value": "${{ inputs.ref_name || github.ref_name }}",
                    "short": true
                  },
                  {
                    "title": "Executed by",
                    "value": "${{ inputs.actor || github.actor }}",
                    "short": true
                  },
                  {
                    "title": "Execution time",
                    "value": "${{ steps.timestamp.outputs.formatted_time }}",
                    "short": true
                  },
                  {
                    "title": "Status",
                    "value": "${{ steps.notify_vars.outputs.status_value }}",
                    "short": true
                  }
                ],
                "footer": "GitHub Actions",
                "footer_icon": "https://github.githubassets.com/favicon.ico",
                "ts": "${{ steps.timestamp.outputs.time }}"
              }
            ]
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}